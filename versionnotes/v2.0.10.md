## changes

- to support readonly filesystems, we now skip writing the settings file if it fails - this is normal in ephemeral environments such as AWS lambda, where writing the settings file is neither possible nor necessary anyway. 